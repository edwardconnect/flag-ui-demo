import styled from 'styled-components'

const PageContainer = styled.div`
  max-width: 1500px;
  margin: 0 auto;
`

const SettingPanesWrapper = styled.div`
  align-items: flex-start;
  display: flex;
  justify-content: center;
  width: 100%;
`

const styles = {
  SettingPanesWrapper,
  PageContainer
}

export default styles
